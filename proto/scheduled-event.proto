syntax = "proto3";
package pb;
message ScheduledEvent {
    string id = 1;
    string name = 2;
    string event = 3;
    string sourceid = 4;
}

message ScheduledEvents {
    repeated ScheduledEvent events = 1;
}